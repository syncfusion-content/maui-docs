---
layout: post
title: Properties in .NET MAUI Pyramid Chart | Syncfusion®
description: This section lists the public API members in the Syncfusion® .NET MAUI Pyramid Chart (SfPyramidChart) API.
platform: maui
control: SfPyramidChart
documentation: ug
---

# API API Reference for .NET MAUI Pyramid Chart

The Pyramid Chart API is documented across the pages below. Each page lists the public properties, methods, and events declared by a related group of classes.

<table>
<tr>
    <th>Page</th>
    <th>Contents</th>
</tr>
<tr valign="top">
    <td><a href="chart" aria-label="SfPyramidChart API reference - container, data binding, and layout">Chart</a></td>
    <td>Provides access to members of the <code>SfPyramidChart</code> control and its <code>ChartBase</code> base class. Use this reference when configuring the <code>pyramid chart container</code>, including its <code>data binding input</code> (<code>ItemsSource</code>, <code>XBindingPath</code>, <code>YBindingPath</code>), <code>color palette</code>, <code>gap</code> between <code>segments</code>, <code>tooltip</code>, <code>legend wiring</code>, <code>animation</code>, and <code>overall layout</code>.</td>
</tr>

<tr valign="top">
    <td><a href="legend" aria-label="Chart legend API reference - shared helper types and appearance resources">Legend</a></td>
    <td>Lists members of the <code>legend</code> and <code>shared helper types</code>, including <code>ChartLegend</code> and any <code>reusable brush</code> or <code>marker helpers</code>. Use this reference for <code>legend configuration</code>, <code>label styling</code>, and other <code>shared appearance resources</code>.</td>
</tr>

<tr valign="top">
    <td><a href="behavior" aria-label="Chart behaviors API reference - tooltips and selection for pyramid segments">Behaviors</a></td>
    <td>Lists members of <code>tooltip</code> and <code>selection behaviors</code> used by the <code>pyramid chart</code>, including <code>ChartTooltipBehavior</code>, <code>ChartSelectionBehavior</code> and its derivatives (<code>DataPointSelectionBehavior</code>, <code>SeriesSelectionBehavior</code>), and the <code>contextual TooltipInfo</code>. Use this reference for <code>enabling tooltips</code> on <code>chart interaction</code> or for <code>selecting</code>/<code>highlighting</code> individual <code>pyramid segments</code>.</td>
</tr>
</table>
