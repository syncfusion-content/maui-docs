---
layout: post
title: Legend and styles in .NET MAUI Cartesian Chart | Syncfusion®
description: This section lists the public API members of the legend, marker settings, line styles, and shared helper types available in the Syncfusion® .NET MAUI Cartesian Chart (SfCartesianChart) API.
platform: maui
control: SfCartesianChart
documentation: ug
---

# Legend and styles API in .NET MAUI Cartesian Chart

This page lists the public properties, methods, and events of the legend, marker settings, line styles, and shared helper types.

<table>
<tr>
    <th>Class</th>
    <th>Description</th>
</tr>

<tr valign="top">
    <td><a href="https://help.syncfusion.com/cr/maui/Syncfusion.Maui.Charts.ChartLegend.html" aria-label="View ChartLegend class in API reference">ChartLegend</a></td>
    <td>Members declared by <code>ChartLegend</code> itself, covering the legend's <code>layout</code>, <code>orientation</code> (horizontal or vertical), <code>background</code> and <code>border styling</code>, <code>placement</code> (top, bottom, left, right, or floating), <code>item spacing</code>, <code>alignment</code>, <code>toggle visibility on series interaction</code>, and the <code>source collection of items or context</code>. Used to customize how <code>series identifiers</code> are displayed alongside the chart.</td>
</tr>

<tr valign="top">
    <td><a href="https://help.syncfusion.com/cr/maui/Syncfusion.Maui.Charts.ChartMarkerSettings.html" aria-label="View ChartMarkerSettings class in API reference">ChartMarkerSettings</a></td>
    <td>Members declared by <code>ChartMarkerSettings</code>, covering <code>marker appearance</code>: <code>shape</code> (circle, square, diamond, triangle, cross, etc.), <code>fill color</code>, <code>stroke brush</code>, <code>stroke thickness</code>, <code>width</code>, <code>height</code>, and <code>offset from the data point</code>. Used to customize the <code>per-point markers</code> for line, area, scatter, fast line, fast scatter, and trendline series.</td>
</tr>

<tr valign="top">
    <td><a href="https://help.syncfusion.com/cr/maui/Syncfusion.Maui.Charts.DoubleRange.html" aria-label="View DoubleRange class in API reference">DoubleRange</a></td>
    <td>Members declared by <code>DoubleRange</code> — a pair of <code>double endpoints</code> representing a <code>numeric interval</code>, such as the <code>minimum</code> and <code>maximum values</code> plotted on an axis or used in a <code>range query</code>. Provides <code>Start</code>, <code>End</code>, and helper methods to compute <code>midpoint</code>, <code>intersection</code>, and <code>containment tests</code> for chart <code>range calculations</code>.</td>
</tr>
</table>
